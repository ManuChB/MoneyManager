import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    customButtonStyle: {
        width: 60,
        position: 'absolute',
        bottom: 10,
        right: 10,
        zIndex: 100
    },
    customLabelStyle: {
        fontSize: 50,
        fontWeight: '900',
        textAlign: 'center',
        marginLeft: 13,
        marginBottom: 5
    }
})

export default styles;